{
  "name": "tresh-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Tresh — döviz/kripto eşik-alarm uygulaması. Monorepo: web (Next.js), mobile (Expo), shared (paylaşılan mantık).",
  "scripts": {
    "dev:web": "pnpm --filter @tresh/web dev",
    "build:web": "pnpm --filter @tresh/web build",
    "dev:mobile": "pnpm --filter @tresh/mobile start",
    "lint:web": "pnpm --filter @tresh/web lint"
  }
}
